#pragma once

namespace FQL
{
    /**
     * Enum holding all the supported primitive data types.
     */
    enum DataType
    {
        TYPE_BOOL = 300,
        TYPE_INT,
        TYPE_DOUBLE,
        TYPE_STRING,
        TYPE_INVALID,
        TYPE_UNKNOWN
    };
}
